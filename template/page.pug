doctype html
html(lang="en")
  head
    title=bookName
  body 
    h1 #{header}
    p #{body}
    if adjecent
      each route in adjecent
        div 
          p #{route.description}
          a(href='/books/' + bookPath + '/' + route.key)
            #{route.key}
